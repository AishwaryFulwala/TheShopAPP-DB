class CartItem {
    constructor(id, qty, price, title, sum) {
        this.id = id;
        this.qty = qty;
        this.price = price;
        this.title = title;
        this.sum = sum;
    }
};

export default CartItem;